import Manager from "./Manager";
import SoundArray from "./SoundArray";

/**
 * @description the audio Module houses all classes used for processing audio.
 */
const Audio = {
    Manager: Manager,
    SoundArray: SoundArray
}

export default Audio;