import ActivityScript from './ActivityScript';
import Mixins from './Mixins';

/**
 * @description the ActivityScript module. Classes for working with activity scripts.
 */
const ActivityScripts = {
    ActivityScript: ActivityScript,
    Mixins: Mixins
}

export default ActivityScripts;